replicaCount: 2

image: __GITHUB_ACTIONS_VAR__

service:
  type: ClusterIP
  containerPort: 80
  port: 5269
  targetPort: 80
  protocol: TCP

managedIdentity:
  name: twitter-bot-msi

aspNetCore:
  environment: Development